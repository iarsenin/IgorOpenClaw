# Cursor Agent Rules — IgorOpenClaw

## What this repo is

This is NOT a software application. It is the **configuration and workspace
repository** for an OpenClaw autonomous AI agent running on a Mac Mini M2
(Apple Silicon, 8 GB RAM, macOS).

OpenClaw is installed globally via npm (`npm install -g openclaw`). This repo
contains:

- `config/openclaw.json.template` — main OpenClaw configuration template (secrets
  injected from `.env` by `scripts/setup.sh` into `~/.openclaw/openclaw.json` —
  generated file is git-ignored)
- `config/riley-voice-behavior.md` — canonical Vapi assistant voicemail + callback policy (merge into Riley's system prompt)
- `workspace/` — OpenClaw agent workspace files (AGENTS.md, SOUL.md, etc.),
  symlinked into `~/.openclaw/workspace/`
- `scripts/` — setup, teardown, and integration helpers (imessage.py, whatsapp.py,
  vapi-call.py, setup.sh, uninstall.sh)
- `SETUP_GUIDE.md` — full installation walkthrough

## Critical rules

1. **Never commit secrets.** All API keys live in `.env` (git-ignored). Config
   files reference them via environment variable names only.
2. **Never hardcode API keys, tokens, or passwords** in any file that gets
   committed.
3. **After editing `config/openclaw.json.template`** (then re-run `setup.sh`) **or**
   **`~/.openclaw/openclaw.json`**, restart the gateway: `openclaw gateway restart`.
4. **After editing any `workspace/*.md` file**, changes take effect on the
   agent's next turn (no restart needed).
5. **Never restart the gateway blindly.** `openclaw gateway restart` sends
   SIGTERM to the live process and kills everything in flight (transcribe
   jobs, active agent turns, vapi calls, browser sessions). Before restarting:
   - Check `/tmp/openclaw/openclaw-$(date +%Y-%m-%d).log | tail -20` for recent
     inbound messages without a matching `auto-reply sent` for that `correlationId`
     — that means a reply is still being worked on.
   - Look for `transcribe-command intercepting before_dispatch` lines
     without a later completion for that URL.
   - If any in-flight work is detected, either wait for it to finish or ask Igor
     before restarting. MEMORY.md / workspace/*.md edits never need a restart;
     only config/cron and gateway-level JSON changes do.
5. **SETUP_GUIDE.md is a standalone doc** meant to be pasted into a Gemini
   session. Keep it self-contained with full commands and context.

## File layout

- `config/openclaw.json.template` — multi-model config template (OpenAI primary,
  Gemini fallback), WhatsApp channel, security settings. Auth token placeholder
  `__OPENCLAW_AUTH_TOKEN__` is injected from `.env` at setup time.
- `config/cron/jobs.json` — scheduled/heartbeat job definitions
- `workspace/AGENTS.md` — operational rules, task routing, and **Questions to the user** (prefer y/n and multiple choice when practical)
- `workspace/SOUL.md` — agent personality and tone
- `workspace/USER.md` — user context (timezone, preferences)
- `workspace/TOOLS.md` — available tools and environment notes
- `workspace/MEMORY.template.md` — tracked template for memory structure
- `workspace/MEMORY.md` — **symlink** → `~/.openclaw/state/MEMORY.md`
  (local-only, git-ignored; agent writes every turn)
- `workspace/memory/` — **symlink** → `~/.openclaw/state/memory/`
  (task-history.md, dated snapshots, .dreams/)
- `workspace/auth-profiles.json` — **symlink** → `~/.openclaw/state/auth-profiles.json`
  (OAuth tokens)
- `workspace/HEARTBEAT.md` — proactive behavior guidelines (references
  config/cron/jobs.json for schedules)
- `.env.example` — template for required environment variables

### Runtime state lives outside Google Drive

Anything the agent writes frequently (MEMORY.md, transcripts, vapi queues,
OAuth tokens, task history) lives under `~/.openclaw/state/` — NOT in this
repo. Drive File Stream throws `OSError: [Errno 11] Resource deadlock avoided`
under concurrent small writes, and pointlessly syncs GB of scratch audio. The
workspace/ entries above are symlinks; `scripts/setup.sh` recreates them on a
fresh install. `scripts/daily-restart.sh` sweeps abandoned transcript workdirs
(>1 day) and prunes `/tmp/openclaw/openclaw-*.log` (>7 days) every morning at
4 AM ET. Transcribe runs self-delete their workdir as soon as the email sends.

## After every change session

1. **Update documentation.** Any change you make — config, scripts, workspace
   files, new skills — must be reflected in the relevant docs:
   - `README.md` — project overview, architecture, repo layout, quick start
   - `SETUP_GUIDE.md` — self-contained installation walkthrough (for Gemini)
   - `.cursorrules` (this file) — file layout, rules, and conventions
   - `workspace/*.md` — if agent behavior or tools changed
   Keep docs accurate so that a **fresh Cursor agent session** (with no memory
   of prior conversations) can understand the full project state by reading them.

2. **Commit and push to GitHub.** After completing changes, stage, commit, and
   push so nothing is lost:
   ```bash
   git add .
   git commit -m "<concise description of what changed and why>"
   git push
   ```
   Never commit `.env` or any file containing secrets.

3. **Restart if needed.** After editing `config/openclaw.json.template` / live
   `~/.openclaw/openclaw.json` or `scripts/`:
   ```bash
   openclaw gateway restart
   ```

## Testing changes

```bash
# Restart gateway after config changes
openclaw gateway restart

# Check gateway health
openclaw gateway status

# View recent logs (rotated daily)
ls -lt /tmp/openclaw/openclaw-*.log | head -3
tail -50 /tmp/openclaw/openclaw-$(date +%Y-%m-%d).log
```

## Current state snapshot

**STATUS: ACTIVE** — Restored 2026-04-18. Gateway, LaunchAgents, cron, and WhatsApp are running.

### Current config (as of 2026-04-19)

- **Primary model:** google/gemini-2.5-pro (OpenAI quota exhausted 2026-04-06; restore with `primary: "openai/gpt-5.4-mini"` after adding credits)
- **Fallback model:** openai/gpt-5.4-mini
- **WhatsApp:** allowlist-only (+19179752041, +19176997436), groups disabled
- **Gateway:** localhost:18789, token auth (token injected from .env via template)
- **Exec config:** `host: "gateway"`, `security: "full"`, pathPrepend (python3, homebrew).
  KEEP `security: "full"` on BOTH `tools.exec` in openclaw.json AND
  `~/.openclaw/exec-approvals.json` (`ask: "off"`, `autoAllowSkills: true`).
  **Agent must NEVER pass `host`, `security`, or `ask` overrides in exec tool calls** — see TOOLS.md § Exec call rules.
- **Browser:** Chrome debug profile at `~/.openclaw/chrome-profile/` (port 9222).
  `refs=aria` is NOT supported (no Playwright `_snapshotForAI`); omit the refs param.
- **Daily restart:** 4 AM ET via launchd (`ai.openclaw.daily-restart`)
- **Cron jobs:** post-restart-resume (4:05 AM), morning briefing (7:30 AM),
  api-spend-check (5:00 AM, 180s timeout — uses OPENAI_ADMIN_KEY; reads Cursor token from local SQLite),
  email triage (every 2h 8AM-10PM), chrono24-listing-monitor (5x/day),
  sms-reply-monitor (~every 2h 8:30AM-8:30PM), inbound-call-check (every 30 min), system-health (5x/day).
  Cron jobs use `--no-deliver`; agent sends WhatsApp via `message` tool explicitly.
  Browser-using crons must end with `browser close`.
- **Workspace file size limit:** Gateway bootstraps workspace files into context with a **12,000-char limit**.
  `AGENTS.md` must stay ≤ 12,000 chars (currently 11,121). `TOOLS.md` must stay ≤ 12,000 chars (currently 11,960).
  Files over the limit are silently truncated — the agent runs without the tail of its rules.
- **Task persistence:** MEMORY.md, TTL 7 days, staleness 48h. See AGENTS.md.
- **Skills (17):** browser-automation, screenshot, coding-agent, gemini, github,
  gh-issues, peekaboo, summarize, healthcheck, openai-image-gen, openai-whisper-api,
  weather, skill-creator, node-connect, gog, himalaya, apple-reminders
- **iMessage/SMS:** scripts/imessage.py — requires Full Disk Access for python3 binary.
- **Phone calls:** scripts/vapi-call.py — Riley assistant (VAPI_ASSISTANT_ID in .env).
  Outbound: +19179628631. Cost ~11c/min. Always ask before dialing.
- **Email:** Gmail works (himalaya + gog OAuth). Yahoo NOT wired (YAHOO_APP_PASSWORD missing from .env).
- **Calendar/Drive/Contacts:** gog CLI (OAuth, project polar-city-407401)
- **GitHub:** https://github.com/iarsenin/IgorOpenClaw (private)

### To restore after future shutdown

1. **Add OpenAI credits** at https://platform.openai.com/settings/billing
   Then restore primary model in `config/openclaw.json.template`:
   `primary: "openai/gpt-5.4-mini"` and `fallbacks: ["google/gemini-2.5-pro"]`

2. **Add Yahoo email creds** — add these two lines to `.env` before running `setup.sh`:
   ```
   YAHOO_USER=arsenin@yahoo.com
   YAHOO_APP_PASSWORD=<your-yahoo-app-password>
   ```

3. Run `scripts/setup.sh` to inject `.env` into the LaunchAgent plist and start the gateway.

4. **Re-pair WhatsApp** — after gateway starts, run:
   ```bash
   openclaw channels login --channel whatsapp
   ```
   If QR code doesn't appear, first clear all Linked Devices on the phone:
   WhatsApp app → Settings → Linked Devices → tap each → Log Out.
